apiVersion: v1
kind: Service

metadata:
  name: backend-service
  namespace: production-grade-app

spec:
  type: LoadBalancer

  selector:
    app: backend

  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
